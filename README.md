# homebrew-zighouse

Homebrew tap for [zighouse](https://github.com/donge/zighouse) — a ClickHouse-compatible columnar OLAP engine.

## Install

```bash
brew tap donge/zighouse
brew install zighouse
brew services start zighouse
```

## Upgrade

```bash
brew update
brew upgrade zighouse
```
